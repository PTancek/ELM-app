-- port module Ports exposing ()

-- import Recipe exposing (..)
-- import Json.Encode as Encode

-- port storeRecipes : List Recipe -> Cmd msg

-- saveRecipes : List Recipe -> Cmd msg
-- saveRecipes posts =
--     Encode.list recipesEncoder posts
--         |> Encode.encode 0
--         |> Ports.storeRecipes

